// Package usecase contains business scenarios and the interfaces they require
// from external dependencies, such as repositories. It depends on domain;
// concrete adapters are injected by app.
package usecase
